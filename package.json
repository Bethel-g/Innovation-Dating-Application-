{
  "name": "innovative-dating",
  "version": "1.0.0",
  "private": true,
  "description": "Innovation Dating — Professional Skills-Based Matching & Collaboration Network",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "client": "cd client && npm run dev",
    "admin": "cd client && npm run admin",
    "mobile": "cd mobile && npx expo start",
    "install:all": "npm install --prefix backend && npm install --prefix client && npm install --prefix admin && npm install --prefix mobile",
    "dev": "concurrently \"npm run backend\" \"npm run client\" \"npm run admin\"",
    "test": "npm test",
    "test:backend": "cd backend && node --experimental-vm-modules --test",
    "test:frontend": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
